<!DOCTYPE html>
<html>
    <head>
        <meta charset='utf-8'>
        <meta http-equiv='X-UA-Compatible' content='IE=edge'>
        <title>Page Title</title>
        <meta name='viewport' content='width=device-width, initial-scale=1'>
        <script src="/js/script.js" defer></script>
        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    </head>
    <body>
        <%- include('partials/header.html') %>
        <div class="container" style="height: 600px">
            <br /> <h2>Olá, <%= usuario.nome %></h2> <br />
            <h3>Deseja editar seu perfil?</h3> <br />
            <form action="/usuario/<%=usuario.id%>?_method=PATCH"
                method="POST" class="form-group col-md-6 center">
                <div class="mb-3">
                    <label for="exampleFormControlInput1" class="form-label">Nome</label>
                    <textarea name="nome" class="form-control" id="exampleFormControlTextarea1" rows="3">
                        <%= usuario.nome %>
                    </textarea>
                </div>
                <div class="mb-3">
                    <label for="exampleFormControlInput1" class="form-label">Login</label>
                    <textarea name="login" class="form-control" id="exampleFormControlTextarea1" rows="3">
                        <%= usuario.login %>
                    </textarea>
                </div>
                <br/><button type="submit" class="btn btn-success">Salvar!</button>
                <a class="btn btn-primary" href="/usuario/logoff">Logoff</a>
                <a href='/usuario/login' class="btn btn-danger" onclick="apagaUsuario(<%=usuario.id%>)">Apagar usuário</a>
            </form>
        </div>
        <%- include('partials/footer.html') %>
    </body>
</html>